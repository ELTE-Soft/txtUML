package hu.elte.txtuml.api.model.seqdiag;

public abstract class Query {

	/*
	 * public static <T extends Signal> T lastReceivedSignal(Class<T>
	 * signalClass) { RuntimeContext context =
	 * RuntimeContext.getCurrentExecutorThread(); BaseInteractionWrapper wrapper
	 * = context.getRuntime().getCurrentInteraction();
	 * 
	 * @SuppressWarnings("unchecked") T element =
	 * (T)wrapper.getMessages().get(wrapper.getMessages().size() -
	 * 1).getSignal(); return element; }
	 */
}
