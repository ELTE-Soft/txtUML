package hu.elte.txtuml.api.diagnostics.protocol;

/**
 * Global protocol and timing related constants which can be used from the back-end as well.
 * @author gerazo
 */
public class GlobalSettings {
	/**
	 * Sets the VM property key which determines the diagnostics back-end port
	 */
	public static final String TXTUML_DIAGNOSTICS_PORT_KEY = "txtUMLDiagnosticsPort";
}
